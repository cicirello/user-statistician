query($owner: String!) {
  user(login: $owner) {
    contributionsCollection(from: "{YEAR}-01-01T00:00:00.001Z") {
      totalCommitContributions
      totalPullRequestReviewContributions
      restrictedContributionsCount
    }
  }
}
